{
  "name": "raymarcher-test",
  "version": "1.0.0",
  "description": "Hello there :)",
  "main": "src/main.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "tsc && vite build",
    "serve": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/three": "^0.181.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.10"
  },
  "dependencies": {
    "gl-matrix": "^3.4.4",
    "three": "^0.181.0"
  }
}
